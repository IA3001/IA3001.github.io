---
title: 2026“钉耙编程”中国大学生算法设计春季联赛（1）
tags:
  - 2026杭电春季多校
  - HDU
published: true
---
## 1001 氟化钙

### Problem Description

给定一个数组 $a$，元素的值 $\ge 2$。经过简单的数学推导，所求答案为 $a_i + a_k = a_j + 1$ 的数量，其中 $i < j < k$。

### Solution

**笛卡尔树、启发式合并**

显然是一个版子题，but……

**踩坑**

- **WA**：由于我**不熟悉** **笛卡尔树**，直接看的**板子**，板子没写**多测的 `clear()` 初始化**，所以没有清空 `lc[i]=rc[i]=0`
  - 这是我**当前板子的通病**，没有做好 `clear()` 的准备
  - 以后实现数据结构，**多测一定记得清空**！
- **TLE**：`std::multiset::count` 函数复杂度为 $O(\log n + k)$，其中 $k$ 为目标元素的真实数量，因此吃了一个
  - 所以推荐使用 `std::map` 而非 `std::multiset` 进行存储！

## 1002 数字王国的警报密码

### Problem Description

给定 $n,k$，构造出**长度为 $n$** 的且**任意子区间的和不等于 $k$** 的**严格单增**的**字典序最小**的序列，不存在输出 $-1$。

数据范围：$\sum k \le 2 \times 10^6$

### Solution

**暴力构造、全局懒标记 Trick**

- 根据鸽巢原理和一些推导，可以知道最多只能有 $k-1$ 长度的解
- 于是就模拟向右扩展新的数字 $p$，扩展步骤如下：
  - 扩展数字时，新的区间为所有以新数字为结尾的区间，需要查询是否存在一个之前序列的后缀和（可以是 0）与新的数字相加恰好模 $k$ 等于 $0$
  - 也就是说，是否存在 $k-p$ 同余的后缀和，即 `pre[((k-p)%k+k)%k]==true`；如果存在，不断的 `++p` 直到不存在对应的"敌人"即可
  - 一旦确定该数字，就需要更新新序列的各种后缀和，显然为 $\{\text{之前的所有后缀和}+p\} \cup \{0\}$，关键的来了！
    - 我们想要求解新的 `pre[]` 结构，绝对不能暴力
    - 通过全局懒标记，即使用一个 `d` 表示全局加，想要让所有元素 `+=v` 直接令 `d+=v` 即可
    - 查询 `v` 是否存在时，由于所要查询的 `v` 其实是在 `pre[]` 中 `+=d` 得到的，所以为了找到它的真实状态需要相应的 `-=d`，即查询 $v-d$

**踩坑**

- **UNK**：由于我**不熟练** **全局懒标记**，导致花了太长时间推导调试！
  - （赛时被卡的地方）关于新元素 $0$ 加入我认为是相反的，就搞反了。**事实上，读/写一个元素，所需要的下标是相同的！**如果想全体加 $p$ 然后新增一个 $0$，很简单，先 $d=(d+p)\%k$（一定要取模啊！），然后 `pre[((0-d)%k+k)%k]=true`
  - **全体加 $v$** 只需要 `tag+=v`！
  - **读/写 $v$** 只需要 `arr[v-tag]`！
- **WA**：平移参数**这种变量**，不断的加数字，必然**可能会爆 `int`**，但是我没有注意

### Code

```cpp
pre[0]=true;
int d=0,p=1;
while(ans.size()<n){
    if(!pre[((k-p-d)%k+k)%k]){
        ans.push_back(p);
        d=(d+p)%k;// 一定要注意啊 如果不取模 或者开 LL 容易爆 int 啊
        pre[((0-d)%k+k)%k]=true;
    }
    p++;
}
```

## 1003 回文串

### Problem Description

在 $S,T$ 中分别选取非空子串 $S',T'$，顺次拼接为 $S'+T'$ 恰好为回文串，求方案数。

数据范围：$n \le 2000$，$T \le 10$

### Solution

**Manacher + DP，$O(n^2)$**

想象一下最终回文串的组成方式（前提：$S' = T'_r$）：

- 要么 $S'+T'$
- 要么 $(S' + \text{S'后附加回文}) + T'$
- 要么 $S' + (\text{T'前附加回文} + T')$

所以翻转一下 $T$ 即可，变为计算所有 $[S' = T'_r] \times \text{计数权重}$。

设：

- $\text{dp}[i][j]$ 表示从 $S[i], T_r[j]$ 开始，合法匹配的方案数，答案即为 $\sum_{1 \le i \le n, 1 \le j \le m} \text{dp}[i][j]$
- $f[i]$ 表示 $S[i]$ 开头作为回文串的方案数
- $g[i]$ 表示 $T_r[i]$ 开头作为回文串的方案数

$$
\text{dp}[i][j] = \begin{cases}
0 & S[i] \ne T_r[j] \\
(1 + f[i+1] + g[j+1]) + \text{dp}[i+1][j+1] & S[i] = T_r[j]
\end{cases}
$$

使用 **Manacher** 计算回文半径，离线区间 +1 即可算出 $f[i], g[i]$。

**踩坑**

- **UNK**：**不熟悉** **exKMP** 模式串与文本串的角色关系，对其 **lcp** 相关内容不清晰，走了好多弯路！
  - 设 `s` 是文本串（`n` 长度），`p` 是模式串（`m` 长度）
  - **`z` 数组**是 `p` 计算出的 `m` 长度的，表示 `p` 的各个位置开始的后缀与 `p` 自身的 **lcp**
  - **`lcp` 数组**是 `s` 计算出的 `n` 长度的，表示 `s` 的各个位置开始的后缀与 `p` 的 **lcp**
- **WA**：没看清 $n \le 2000$，自以为是地写成了 `const int N = 1e3 + 5`，卡了好久

## 1004 炼金术士的配方

### Problem Description

给出一个长度为 $n$ 数组 $a$，求满足"区间乘积后仅保留奇数次幂的质数的去重集合与 $x$ 的集合相等"的子区间 $[l,r]$ 数量。

（另一种阐述）`bitset<1e6> A[5e5]` 区间异或和等于给定 `bitset<1e6> X` 的方法数。

数据范围：$2 \le n \le 5 \times 10^5$，$1 \le a[i] \le 10^6$，$1 \le x \le 10^6$

### Solution

**计数、随机化映射、01集合压缩映射**

- 首先，传统的维护前缀异或和的方式复杂度太大，其瓶颈在于：
  - 虽然初始每个 $A[i]$ 中 1 的数量是 log 级别的，但是合并会导致 1 状态达到 $n$ 级别
- 我们尝试化简，压缩集合使其变得精简，这需要随机化映射的辅助，保证低出错概率
  - 即传统的集合存储方式，我们使用了信息保留量 100% 的方式
  - 现在将一个大的 $O(n)$ 级别的集合映射为一个 $O(1)$ 级别的哈希值，同时要满足易于维护任意质数的插入与删除
  - 显然，集合全体值的异或是一个不错的方式：当插入/删除一个质数时，只需要异或上它的基础映射值即可
- 但是现在我不会证明它的准确性 QAQ

## 1006 巧克力工厂

### Problem Description

非常简单的签到题，题面是一种流水线。

**踩坑**

- **UNK**：我读的太快，以为是一般的任务分派，看错了，写了个排序，没过样例，浪费好长时间，故记录此事
  - **无论**是比赛刚开始，还是比赛中期，还是比赛结束前
  - **不要着急，仔细读题！**

## 1007 流沙

### Problem Description

简单的树上 DP 问题。

**踩坑**

- **WA**：依然犯了个低级错误，样例竟然没有检查出来
  - 树上 DP dfs 收集信息的时候，忘记收集 `sz[u]+=sz[v]`
  - 以后可别再丢三落四了，应对策略：
    - 提交之前**按照拓扑**过一遍各个计算所需要的所有依赖是否全部计算完毕

## 1009 力的平衡

### Problem Description

给定 $k \le 500$ 个规模 $\le 100$ 二维的力 $F$，可以重复使用，求最小的合力为 $0$ 所需的使用量。

数据范围：$\sum k \le 1000$，$T \le 10$

### Solution

**合理地缩小判定范围、BFS**

- 由于向量的合成，力自然可以围成一个凸包环，但是合成过程中，可能会达到距离原点很远的地方。
- 注意到**不需要严格规定使用次序**，且**单个向量的移动能力有限**，完全可以划定一个足够小的有限的定义域范围，使得所有力的合成都在定义域范围内进行（类比挪车的辅助空间）
- 故以单个力的规模 $100$ 为块单位，两个维度范围均设置为 $4$ 个单位块长度左右即可

## 1010 MEX

### Problem Description

给出 $m$ 个关于值域为 $[0,n-1]$ 排列数组 $a[1 \dots n]$ 的信息 $x_i,y_i$，表示 $\text{mex } a[x \dots n] = y$，求数组 $a$ 的可能方案数。

### Solution

**计数、排列组合**

不想不知道，一想就知道。当你手玩，尝试从 $p=n+1$（表示已经填过数字的边界）开始填数字的时候，$\text{cur}=0$（已经填过的数字的 mex 值），你就会发现：
- 可选数字会被 $y[1 \dots p]$ ban 掉，否则不符合 mex 定义
- 并且在处理 $x_i,y_i$ 时，必须保证在 $a[x_i \dots p-1]$ 区间内所有的值域在 $[\text{cur}, y-1]$ 的数字都必须已经被使用，有一个非常细节的地方：
  - 注意到区间 $[\text{cur}, y-1]$ 中，只有 $\text{cur}$ 是之前一直被 ban 掉的
  - （$\text{cur} < y-1$ 的情况）$\text{cur}$ 必须在这一段区间内被选择（被"安排"了），而 $[\text{cur}+1, y-1]$ 的数字，则必然不是被 ban 掉的，所以可以在之前的任意未被"安排"操作的位置填充，然后那些**未被"安排"的位置也被设置为"安排"**
  - 所以需要维护一个被"安排"的余量 $r$

### Code

```cpp
int r=n;
int cur=0,p=n+1;
LL ans=1;
for(int i=m;i>=1;i--){
    auto [x,y]=xy[i];
    int l = p-x;
    int h = y-cur;
    if(h>0)r--;
    LL tk=C(l,h>0)*A(r-x+1,h-(h>0))%mod;
    r-=(h-(h>0));// 要减掉 已经使用过的 x 位置 
    ans=ans*tk%mod;
    p=x;
    cur=y;
}
```
