---
title: 2026“钉耙编程”中国大学生算法设计春季联赛（2）
tags:
  - 2026杭电春季多校
  - HDU
published: true
---
## 1003 A 和 B 依次选数 选完后立刻删除该数

### Problem Description

有一个长度为 $n$ 的序列 $a$，$f(k)$ 为以下循环后最终 A 可以获得的最大值：
- A 先手拿走任意一个 $a$ 中剩余的数字
- B 后手拿走 $a$ 中前 $k$ 个数字

对于 $k = 1, 2, \dots, n-1$ 求 $f(k)$。

数据范围：$2 \le n \le 2 \times 10^5$，$1 \le a_i \le 10^9$

### Solution

**限制转化、调和级数、（可撤销）单点修改线段树 /（可撤销）快速删除数组**

先看这个单人博弈是怎么转化的：
- 首先可以知道：前 $k+1$ 个数最多只能选 1 个，前 $2k+2$ 个数最多选 2 个……
- A 最终选择了 $\text{num} = \lceil n / (k+1) \rceil$ 个数（**调和级数**已经出现了！！！）
- 所以什么意思呢？画个图
- 你会发现可以使用 $\text{num}$ 个后缀区间即可转化为：只能使用这 $\text{num}$ 个区间来获取数字，最终的最大值

显然决策可以这样做：
- 因为最左边的自由度最大，所以最后决策
- 从右往左决策，每次取当前后缀集合中元素的最大值即可！
- **决策多少次**？$O(\text{num}) = O(n/k)$，**调和级数**！！！

决策所需要的数据结构要求是什么样的？
- 需要在一个**数组**中**区间查询最大值**
- 需要在一个**数组**中**单点删除**
  - 什么？删除？难道是集合、堆的操作？？？
  - 不！！！**线段树**也可以删除！
  - **什么是线段树的删除？**
    - **删除就是让这个元素成为答案的优先级最小！！！（即等价于修改操作！！！）**
  - **线段树既然可以删除，如何复原？**
    - 用一个栈维护，不久实现带撤销了吗！！！
- 当时莫名觉得线段树节点维护单个区间 $\max$ 值有点诡异

**快速删除数组**的基本 Trick：
- 往往要查询区间最值
- 在删除最值的时候可能有多个，若要只删除最左/最右的一个
- 所以往往需要增加一维 index，即 `pair<T, int>` 作为线段树节点值

**踩坑**

- **UNK**：我做这道题的时候，首先看到 2e5 和 6000ms 竟然想的是根号，就往根号分治上去想了，先是单个线性暴力写出来前根号个，然后根据根号分治经典的 Trick，大于 sqrt 的答案往往是单调且答案连续重复的，所以就去二分 sqrt 个答案范围，之前没想到怎么近似线性求解单个值，以为是反悔贪心需要借助堆然后模拟，后来才知道，有一种公式化的求法，就是从中看出来有以 $k+1$ 为步长的槽（即 num 个可选区间，每次使用一个区间，选择区间内的一个数字），共 num 个，这样就完全满足博弈的限制了，可以直接简单的求，但是一旦知道了这个公式，我竟然直接就按部就班了，没想再去优化，也不知怎么优化，好长时间后，我才想到一个倒序插入元素到大根堆中，在每一个槽的临界点处选择一个最大的元素并删除这个元素，复杂度直接 $O(n \sqrt{n} \log^2 n)$ 了，如果再用并查集的话可以减少一个 log，没想到还是没有通过，我又想了想，原先堆的模拟中，显然是需要一个在指定区间内查询最大值、删除某个值，我竟然没有想到线段树，只是**傻傻的通过 push 堆不断的扩展区间**，并没有想到使用**线段树预处理区间**！！！我竟然把线段树的用法忘了！竟然不会用线段树了？！
- **UNK**：想想都不敢想的东西竟然敢想
  - $O(n \sqrt{n} \log n)$ ???
  - 虽然是 6000ms 的时限，但也是远远不够啊！后面的都乘到 750 x 20 了都
  - 但是你不想想 $O(n \log^2 n)$ 的复杂度吗？？？这里顶多乘 20 x 20
- **UNK**：别被**单点修改线段树**骗了，**线段树是可以有"删除"操作的！！！**
- **TLE**：被**根号分治**蒙蔽了双眼，又被**调和级数**骗了，没去深究复杂度
- **IDEA**：这个单人的博弈，以为是**贪心反悔**，竟然是**转化后**一个简单的**公式化限制**！

### Code

```cpp
// 快速删除数组
// 插入 索引
void insert(int i,int l,int r,int p) {
    if(l==r){
        S[i] = {a[p], p};
    } else {
        int mid = (l+r)/2;
        if(p<=mid)insert(i*2,l,mid,p);
        else insert(i*2+1,mid+1,r,p);
        up(i);
    }
}
// 删除 索引
void erase(int i,int l,int r,int p) {
    if(l==r){
        S[i] = {0,0};
    }else {
        int mid = (l+r)/2;
        if(p<=mid)erase(i*2,l,mid,p);
        else erase(i*2+1,mid+1,r,p);
        up(i);
    }
}
// code
for(int i = num; i >= 1;i--){
    // 分块的 
    int st = (k + 1) * (i - 1) + 1;
    // 查询当前后缀集合最大值 
    auto [val, idx] = T.query(1,1,n,st,n);
    res += val;
    // 记录一下 被删除的索引
    T.erase(1,1,n,idx);
    stk[++tp] = idx;
}
// 恢复集合 
while(tp) T.insert(1,1,n,stk[tp--]);
```
